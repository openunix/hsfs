# This is the must have feature that fuse needed.
if ENABLE_MEMCACHED
  MEMCACHED_FLAGS = -DCONFIG_MEMCACHED=1
else
  MEMCACHED_FLAGS =
endif

AM_CPPFLAGS = -D_FILE_OFFSET_BITS=64 $(TIRPC_HEADERS) $(MEMCACHED_FLAGS)
AM_CFLAGS = -Wall -Wextra
